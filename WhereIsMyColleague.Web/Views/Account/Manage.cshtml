@model WhereIsMyColleague.Web.Models.LocalPasswordModel
@{
  ViewBag.Title = "Manage Account";
}

<hgroup class="title">
  <h1 class="manage-header">@ViewBag.Title.</h1>
</hgroup>

<p class="message-success">@ViewBag.StatusMessage</p>

<p class="p-font">You are logged in as <strong>@User.Identity.Name</strong>.</p>

@if (ViewBag.HasLocalPassword)
{
  @Html.Partial("_ChangePasswordPartial")
}
else
{
  @Html.Partial("_SetPasswordPartial")
}

<section id="externalLogins">
  @Html.Action("RemoveExternalLogins")

  <h3 class="add-external-header">Add an external login</h3>
  @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
</section>